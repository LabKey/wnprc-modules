import * as React from 'react';
import { FC } from 'react';
import '../../../cageui.scss';

export const RoomDetails: FC = () => {

    return (
        <div>
            Room Details
        </div>
    );
};