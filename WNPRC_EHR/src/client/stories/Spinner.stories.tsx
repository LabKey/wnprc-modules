import * as React from 'react';
import  Spinner  from '../components/Spinner';

export default {
    title: 'Spinner',
    component: Spinner,
};

export const ToStorybook = () => <Spinner text={"Test"}/>;
