import org.labkey.gradle.util.BuildUtils
apply plugin: 'org.labkey.build.module'

dependencies {
    BuildUtils.addLabKeyDependency(project: project, config: "implementation", depProjectPath: "${project.parent.path}:DBUtils", depProjectConfig: 'apiJarFile')
    BuildUtils.addLabKeyDependency(project: project, config: "implementation", depProjectPath: ":server:modules:ehrModules:ehr", depProjectConfig: 'apiJarFile')

    //works
    implementation project(":server:modules:wnprc-modules:WNPRC_EHR")
    //does not work
    //BuildUtils.addLabKeyDependency(project: project, config: "implementation", depProjectPath: "${project.parent.path}:WNPRC_EHR", depProjectConfig: 'apiJarFile')
    //BuildUtils.addLabKeyDependency(project: project, config: "implementation", depProjectPath: "${project.parent.path}:WNPRC_EHR", depProjectConfig: 'module')
    //BuildUtils.addLabKeyDependency(project: project, config: "modules", depProjectPath: "${project.parent.path}:WNPRC_EHR", depProjectConfig: 'published', depExtension: 'module')
}
