dependencies {
    apiExternal fileTree("${project.projectDir}/../lib")
    compile "javax.servlet:jsp-api:2.0"
    compile project(path: "${project.parent.path}:WebUtils", configuration: "apiCompile")
    external fileTree("${project.projectDir}/../lib")
}
